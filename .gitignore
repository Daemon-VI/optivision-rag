.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# generated artefacts — reproducible with `make corpus` / `make bench`
data/
reports/bench_indexes/
!reports/.gitkeep

# embedded Qdrant leaves these where a client is opened
.lock
collection/

# Credentials. Nothing here today, but an .env created later would otherwise be
# picked up by the first `git add -A`. Templates stay committable.
.env
.env.*
!.env.example
